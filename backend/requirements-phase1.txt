# ============================================================
# Government Scheme AI Assistant — Phase 1 Requirements
# Web foundation only. No ML, no Celery, no heavy packages.
# ============================================================

# ── Django Core ──
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0
django-filter==24.3

# ── Authentication ──
djangorestframework-simplejwt==5.3.1
PyJWT==2.10.1

# ── Database (psycopg v3 — pure Python, supports Python 3.14) ──
psycopg>=3.1.0

# ── Environment ──
python-decouple==3.8

# ── Security ──
argon2-cffi==23.1.0

# ── API Documentation ──
drf-spectacular==0.27.2

# ── WSGI Server (production) ──
gunicorn==23.0.0
